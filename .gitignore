
.DS_Store
**/.DS_Store
# 検証用の一時ファイル。git add -A で巻き込んで公開してしまった実績あり（2026-07-22）。
# __p.html はLPの完全な複製で、重複コンテンツとして検索に拾われる危険があった。
__*.html

# OGカード生成の副産物（逐語照合用）。リポジトリに置く必要が無いのに未追跡で残り、
# 2026-09-09 に publish_diary.py が rc=2、IGカルーセル20:33枠が HostError で連鎖停止した。
# 出力先は make_og_card.py --headlines-out で MC state/news_og/ へ移したが、
# 旧残骸と取りこぼしがレーンを止めないよう二重の保険として無視する。
news/og/*_headlines.txt
